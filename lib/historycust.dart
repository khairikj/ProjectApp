import 'package:flutter/material.dart';

class HistoryCustomer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('History for customer'),
    );
  }
}
